;(function (window, $, App) {

    'use strict';

    App.Routes = {
        // Fired in all pages
        'init': function  () {
        	SolidGlobal.init()
        }
        
    };

})(window, jQuery, App);